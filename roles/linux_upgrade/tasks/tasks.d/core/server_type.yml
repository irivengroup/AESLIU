- name: Resolve effective server type
  set_fact:
    linux_upgrade_server_type_effective: >-
      {% if linux_upgrade_server_type != 'auto' %}
      {{ linux_upgrade_server_type }}
      {% elif (ansible_virtualization_role | default('')) == 'guest' %}
      vm
      {% else %}
      physical
      {% endif %}
