- name: Read VMware secrets from Vault
  set_fact:
    linux_upgrade_vmware_hostname: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.vmware, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.hostname | default(linux_upgrade_vmware_hostname, true) }}"
    linux_upgrade_vmware_username: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.vmware, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.username | default(linux_upgrade_vmware_username, true) }}"
    linux_upgrade_vmware_password: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.vmware, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.password | default(linux_upgrade_vmware_password, true) }}"
  no_log: true
  when: linux_upgrade_vault_enabled | bool

- name: Read Proxmox secrets from Vault
  set_fact:
    linux_upgrade_proxmox_api_host: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.proxmox, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.api_host | default(linux_upgrade_proxmox_api_host, true) }}"
    linux_upgrade_proxmox_api_user: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.proxmox, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.api_user | default(linux_upgrade_proxmox_api_user, true) }}"
    linux_upgrade_proxmox_api_password: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.proxmox, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.api_password | default(linux_upgrade_proxmox_api_password, true) }}"
  no_log: true
  when: linux_upgrade_vault_enabled | bool

- name: Read OpenStack secrets from Vault
  set_fact:
    linux_upgrade_openstack_cloud: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.openstack, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.cloud | default(linux_upgrade_openstack_cloud, true) }}"
  no_log: true
  when: linux_upgrade_vault_enabled | bool

- name: Read ServiceNow secrets from Vault
  set_fact:
    linux_upgrade_servicenow_url: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.servicenow, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.url | default(linux_upgrade_servicenow_url, true) }}"
    linux_upgrade_servicenow_username: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.servicenow, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.username | default(linux_upgrade_servicenow_username, true) }}"
    linux_upgrade_servicenow_password: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.servicenow, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.password | default(linux_upgrade_servicenow_password, true) }}"
  no_log: true
  when: linux_upgrade_vault_enabled | bool

- name: Apply VMware local secrets fallback
  set_fact:
    linux_upgrade_vmware_hostname: "{{ linux_upgrade_vmware_hostname | default(linux_upgrade_local_secrets.vmware.hostname, true) }}"
    linux_upgrade_vmware_username: "{{ linux_upgrade_vmware_username | default(linux_upgrade_local_secrets.vmware.username, true) }}"
    linux_upgrade_vmware_password: "{{ linux_upgrade_vmware_password | default(linux_upgrade_local_secrets.vmware.password, true) }}"
  no_log: true
  when:
    - linux_upgrade_local_secrets_enabled | bool
    - linux_upgrade_local_secrets is defined
    - linux_upgrade_local_secrets.vmware is defined

- name: Apply Proxmox local secrets fallback
  set_fact:
    linux_upgrade_proxmox_api_host: "{{ linux_upgrade_proxmox_api_host | default(linux_upgrade_local_secrets.proxmox.api_host, true) }}"
    linux_upgrade_proxmox_api_user: "{{ linux_upgrade_proxmox_api_user | default(linux_upgrade_local_secrets.proxmox.api_user, true) }}"
    linux_upgrade_proxmox_api_password: "{{ linux_upgrade_proxmox_api_password | default(linux_upgrade_local_secrets.proxmox.api_password, true) }}"
  no_log: true
  when:
    - linux_upgrade_local_secrets_enabled | bool
    - linux_upgrade_local_secrets is defined
    - linux_upgrade_local_secrets.proxmox is defined

- name: Apply OpenStack local secrets fallback
  set_fact:
    linux_upgrade_openstack_cloud: "{{ linux_upgrade_openstack_cloud | default(linux_upgrade_local_secrets.openstack.cloud, true) }}"
  no_log: true
  when:
    - linux_upgrade_local_secrets_enabled | bool
    - linux_upgrade_local_secrets is defined
    - linux_upgrade_local_secrets.openstack is defined

- name: Apply ServiceNow local secrets fallback
  set_fact:
    linux_upgrade_servicenow_url: "{{ linux_upgrade_servicenow_url | default(linux_upgrade_local_secrets.servicenow.url, true) }}"
    linux_upgrade_servicenow_username: "{{ linux_upgrade_servicenow_username | default(linux_upgrade_local_secrets.servicenow.username, true) }}"
    linux_upgrade_servicenow_password: "{{ linux_upgrade_servicenow_password | default(linux_upgrade_local_secrets.servicenow.password, true) }}"
  no_log: true
  when:
    - linux_upgrade_local_secrets_enabled | bool
    - linux_upgrade_local_secrets is defined
    - linux_upgrade_local_secrets.servicenow is defined


- name: Read Jira secrets from Vault
  set_fact:
    linux_upgrade_jira_url: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.jira, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.url | default(linux_upgrade_jira_url, true) }}"
    linux_upgrade_jira_username: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.jira, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.username | default(linux_upgrade_jira_username, true) }}"
    linux_upgrade_jira_password: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.jira, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.password | default(linux_upgrade_jira_password, true) }}"
    linux_upgrade_jira_token: "{{ lookup('community.hashi_vault.vault_kv2_get', linux_upgrade_vault_secret_paths.jira, engine_mount_point=linux_upgrade_vault_kv_mount, url=linux_upgrade_vault_url, auth_method=linux_upgrade_vault_auth_method, token=linux_upgrade_vault_token | default(omit), role_id=linux_upgrade_vault_role_id | default(omit), secret_id=linux_upgrade_vault_secret_id | default(omit), mount_point=(linux_upgrade_vault_mount_point if linux_upgrade_vault_auth_method == 'approle' else omit), jwt=linux_upgrade_vault_jwt | default(omit), validate_certs=linux_upgrade_vault_validate_certs).secret.token | default(linux_upgrade_jira_token, true) }}"
  no_log: true
  when: linux_upgrade_vault_enabled | bool


- name: Apply Jira local secrets fallback
  set_fact:
    linux_upgrade_jira_url: "{{ linux_upgrade_jira_url | default(linux_upgrade_local_secrets.jira.url, true) }}"
    linux_upgrade_jira_username: "{{ linux_upgrade_jira_username | default(linux_upgrade_local_secrets.jira.username, true) }}"
    linux_upgrade_jira_password: "{{ linux_upgrade_jira_password | default(linux_upgrade_local_secrets.jira.password, true) }}"
    linux_upgrade_jira_token: "{{ linux_upgrade_jira_token | default(linux_upgrade_local_secrets.jira.token, true) }}"
  no_log: true
  when:
    - linux_upgrade_local_secrets_enabled | bool
    - linux_upgrade_local_secrets is defined
    - linux_upgrade_local_secrets.jira is defined
